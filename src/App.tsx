import * as React from 'react';
import CountryTable from './containers/CountryTable';

const App = (): JSX.Element => {
  return (
    <div>
      <CountryTable />
    </div>
  );
};

export default App;
